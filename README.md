
# at — حساب التأخير (iOS-style)

تطبيق ويب يشبه تطبيقات iOS 18/26، لحساب التأخير اليومي عن الدوام بناءً على آخر وقت حضور **07:15 صباحًا**.  
لغة وواجهة عربية (RTL) — بدون أي باك-إند، ويعتمد على LocalStorage.

## الفكرة
- تدخل وقت وصولك اليوم.
- إذا كان الوقت **≤ 07:15** ➜ يظهر "لا يوجد تأخير 🎉".
- إذا كان الوقت **> 07:15** ➜ يحسب الفرق بالدقائق ويعرضه.
- يتم **حفظ سجل اليوم تلقائيًا** في LocalStorage (على نفس المتصفح/الجهاز).

## طريقة الحساب
- وقت مسموح ثابت: `07:15`.
- التأخير = دقائق(وقت الوصول) − دقائق(07:15).
- في حال عدم وجود تأخير، يتم حفظ قيمة 0.

## حفظ البيانات (LocalStorage)
- المفتاح: `attendance_log`
- الصيغة: مصفوفة من العناصر:
  ```json
  [
    { "date": "YYYY-MM-DD", "time": "HH:MM", "delay": 5 }
  ]
  ```
- الحفظ محلي على نفس المتصفح. يتم فقد البيانات عند:
  - حذف بيانات المتصفح/الكوكيز/التخزين.
  - استخدام وضع خاص قد لا يحفظ.
  - فتح الموقع من جهاز/متصفح آخر.

## التشغيل محليًا
```bash
npm i
npm run dev
# افتح http://localhost:3000
```

## النشر على Vercel
1. اربط المستودع على GitHub.
2. أنشئ مشروع في Vercel واختر المستودع.
3. إعدادات افتراضية لــ Next.js (App Router) تكفي.
4. انشر وسيعمل فورًا.

## النشر على Render (Web Service)
- استخدم خدمة **Web Service** Node:
  - **Build Command**: `npm run build`
  - **Start Command**: `npm start`

## التقنية
- Next.js (App Router)
- Tailwind CSS + رموز iOS
- Framer Motion (انتقالات/ضغط الأزرار)
- LocalStorage للسجل
- تصميم iOS: زجاج/blur، زوايا 24–32px، ظلال ناعمة

## ملاحظات
- الوضع الفاتح فقط.
- اتجاه الصفحة RTL افتراضيًا.
- الخط: Tajawal (بديل Cairo).
